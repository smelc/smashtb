if [ -d "$PWD/_opam" ]; then
  eval "$(opam env --switch="$PWD" --set-switch)"
else
  log_status "smashtb: no local opam switch yet, run ./setup.sh"
fi

# Read GITHUB_TOKEN from an untracked .env file if there is one
dotenv_if_exists .env
